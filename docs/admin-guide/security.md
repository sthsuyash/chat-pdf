# Admin security

Purpose: security controls and operational hardening for platform operators.

Key controls

- Enforce least privilege for service accounts and API keys.
- Rotate secrets and provider keys on a regular schedule.
- Use environment-based secrets storage (Vault, cloud KMS).

Operational checks

- Verify audit logs and access logs are enabled.
- Ensure backups are encrypted and access-limited.
