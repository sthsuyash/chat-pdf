# User management

Purpose: admin operations for creating, updating, and revoking users and roles.

Procedures

1. Create users using the admin UI or via the backend API.
2. Assign roles and verify permission boundaries in the UI.
3. Revoke access by disabling accounts or rotating tokens.

Validation

- Changes must be reflected immediately in the admin UI and API responses.
